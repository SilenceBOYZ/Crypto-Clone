function Title({ title, paragraph }) {
  return (
    <>
      <h2 className="text-5xl font-bold">{title}</h2>
      <p className="textColorPrimary text-xl">{paragraph}</p>
    </>
  )
}

export default Title
